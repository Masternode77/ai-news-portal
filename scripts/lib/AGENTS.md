# Content Generation Module Instructions

This is a fallback pointer for tools that read `AGENTS.md` directly. Codex selects the local `AGENTS.override.md` instead when it exists. Follow the repository root `AGENTS.md` and the local editorial override in `AGENTS.override.md`; this pointer adds no separate approval or execution rule.

`AGENTS.override.md` contains the Compute Current content generation acceptance criteria for extraction QA, source fidelity, repetition checks, prompt/fallback copy, and publish readiness.
