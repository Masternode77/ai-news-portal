# Content Generation Module Instructions

For files in this directory, follow the repository root `AGENTS.md` and the local editorial override in `AGENTS.override.md`.

`AGENTS.override.md` contains the Compute Current content generation acceptance criteria for extraction QA, source fidelity, repetition checks, prompt/fallback copy, and publish readiness.
