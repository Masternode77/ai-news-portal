---
const { category, date, source, title, deck, author = null, breadcrumb = null } = Astro.props;
const formattedDate = date
  ? new Date(date).toLocaleDateString('en-US', { month: 'long', day: 'numeric', year: 'numeric' })
  : '';
const crumb = breadcrumb || { label: 'Latest analysis', href: '/' };
---
<header class="article-header">
  <nav class="article-breadcrumb" aria-label="Breadcrumb">
    <a href="/">Compute Current</a>
    <span aria-hidden="true">/</span>
    <a href={crumb.href}>{crumb.label}</a>
  </nav>
  <div class="meta-strip article-meta-strip">
    {category ? <span class="category-badge">{category}</span> : null}
    {formattedDate ? <time datetime={date}>{formattedDate}</time> : null}
    {source?.name ? <span>{source.name}</span> : null}
  </div>
  <h1>{title}</h1>
  {deck ? <p class="detail-summary">{deck}</p> : null}
  {author?.name ? (
    <p class="byline">
      By <a href={author.slug ? `/author/${author.slug}/` : '/about/'}>{author.name}</a>{author.role ? ` · ${author.role}` : ''}
    </p>
  ) : (
    <p class="byline">By Compute Current Editorial Desk</p>
  )}
</header>
