---
const { canonicalUrl = '' } = Astro.props;
---
{canonicalUrl ? (
  <footer class="detail-section article-canonical-footer">
    <p class="canonical-note">Canonical article: <a href={canonicalUrl}>{canonicalUrl}</a></p>
  </footer>
) : null}
