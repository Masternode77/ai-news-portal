# Compute Current Editorial OS

This optional repo-local plugin packages the canonical Compute Current skills under
`skills/`. The files under `commands/` are reference playbooks used by those skills and
operators; they are not independently registered executable Codex commands.

Use the repository npm scripts named in each skill or playbook for execution. The plugin
does not install third-party code, request credentials, or run hooks automatically.
